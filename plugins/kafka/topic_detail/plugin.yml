name: kafka_topic_detail
version: "1.0"
description: Kafka topic 详情（分区、副本、ISR）
type: query

target:
  type: kafka

input:
  kafka:
    type: string
    required: true
    description: kafka.yaml 中的实例 name
  topic:
    type: string
    required: true
    description: topic 名称
runtime: javascript
timeout: 30s
