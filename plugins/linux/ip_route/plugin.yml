name: linux_ip_route
version: "1.0"
description: 查看路由表（ip route）
type: command
target:
  type: ssh
input:
  host:
    type: string
    required: true
    description: hosts.yaml 中的主机 name
runtime: javascript
timeout: 10s
