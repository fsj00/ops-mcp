name: linux_systemctl_failed
version: "1.0"
description: 列出失败的 systemd 单元（systemctl --failed）
type: command
target:
  type: ssh
input:
  host:
    type: string
    required: true
    description: hosts.yaml 中的主机 name
runtime: javascript
timeout: 10s
