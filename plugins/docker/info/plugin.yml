name: docker_info
version: "1.0"
description: 在指定主机上通过 SSH 执行 docker info，查看 Docker 守护进程信息
type: command
target:
  type: docker
input:
  host:
    type: string
    required: true
    description: hosts.yaml 中的主机 name
runtime: javascript
timeout: 15s
